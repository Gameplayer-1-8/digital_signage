{
  "name": "digital-signage-monorepo",
  "version": "1.0.2",
  "private": true,
  "scripts": {
    "dev:backend": "pnpm --filter backend run dev",
    "dev:frontend": "pnpm --filter frontend run dev",
    "dev:android": "pnpm --filter android-client run start",
    "dev": "pnpm --filter backend --filter frontend --parallel run dev"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
